Here we are gonna learn about list rendering
We are gonna display a list of products over here
We should get the data from an array of objects
We are gonna use map() function to create an element for each element present in the array
But whenever we use map() function, to uniquely identify the children of the array, every child in the list should contain a unique key property