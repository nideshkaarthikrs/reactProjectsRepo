A state is an js object/data that is privately maintained inside a component
State is managed within a component
Props can only send static data, i.e they are immutable...
But state is modifiable

State management in class based component:
__________________________________________
(State is defined inside a constructor in a class based component)

Step 1: Create a state object and initialize it normally inside the constructor
Step 2: Bind the state value in the render() function   
Step 3: HTML Button
Step 4: Listen to the click even on the button and change the state value
